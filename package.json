{
  "name": "tradebrain",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate-repository.mjs",
    "build": "node scripts/build-site.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}

name: Deploy TradeBrain Pages

on:
  push:
    branches: [main]
    paths:
      - "providers/chatgpt/snapshots/**/*.json"
      - "providers/claude/snapshots/**/*.json"
      - "providers/*/signal.json"
      - "prompts/**"
      - "index.html"
      - "assets/**"
      - "config/**"
      - "schemas/**"
      - "scripts/**"
      - "test/**"
      - ".nojekyll"
      - ".github/workflows/deploy-pages.yml"
      - "package.json"
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: tradebrain-pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
      - name: Finalize pending ChatGPT snapshots
        id: finalize_chatgpt
        continue-on-error: true
        run: node scripts/finalize-chatgpt.mjs
      - name: Warn if ChatGPT finalization failed
        if: steps.finalize_chatgpt.outcome == 'failure'
        run: echo "::warning::ChatGPT finalization failed; Pages will continue with any provider state that still validates."
      - name: Commit finalized ChatGPT provider state
        shell: bash
        run: |
          if ! git diff --quiet -- providers/chatgpt/history.json providers/chatgpt/status.json providers/chatgpt/signal.json; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add providers/chatgpt/history.json providers/chatgpt/status.json providers/chatgpt/signal.json
            git commit -m "data(chatgpt): finalize provider publication"
            git pull --rebase origin main
            git push origin HEAD:main
          fi
      - name: Finalize pending Claude snapshots
        id: finalize_claude
        continue-on-error: true
        run: node scripts/finalize-claude.mjs
      - name: Warn if Claude finalization failed
        if: steps.finalize_claude.outcome == 'failure'
        run: echo "::warning::Claude finalization failed; Pages will continue with any provider state that still validates."
      - name: Commit finalized Claude provider state
        shell: bash
        run: |
          if ! git diff --quiet -- providers/claude/history.json providers/claude/status.json providers/claude/signal.json; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add providers/claude/history.json providers/claude/status.json providers/claude/signal.json
            git commit -m "data(claude): finalize provider publication"
            git pull --rebase origin main
            git push origin HEAD:main
          fi
      - name: Validate repository for isolated Pages build
        run: node scripts/validate-repository.mjs --isolate-providers
      - name: Run tests
        run: node --test
      - name: Build static site
        run: node scripts/build-site.mjs
      - name: Configure Pages
        uses: actions/configure-pages@v6
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: dist
      - name: Deploy Pages
        id: deployment
        uses: actions/deploy-pages@v5
